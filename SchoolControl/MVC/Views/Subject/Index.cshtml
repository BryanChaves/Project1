@model IEnumerable<Data.Subject>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Subject1)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Subject1)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Subject1 }) |
                @Html.ActionLink("Details", "Details", new { id = item.Subject1 }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Subject1 })
            </td>
        </tr>
    }

</table>
