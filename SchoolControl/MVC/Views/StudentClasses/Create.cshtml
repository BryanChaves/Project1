@model Data.StudentClasses

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>StudentClasses</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.id_student, "Student")
        </div>
        <div class="editor-field">
            @Html.DropDownList("id_student", String.Empty)
            @Html.ValidationMessageFor(model => model.id_student)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.id_class, "Classes")
        </div>
        <div class="editor-field">
            @Html.DropDownList("id_class", String.Empty)
            @Html.ValidationMessageFor(model => model.id_class)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
