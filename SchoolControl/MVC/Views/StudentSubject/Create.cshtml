@model Data.StudentSubject

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>StudentSubject</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Student, "Student1")
        </div>
        <div class="editor-field">
            @Html.DropDownList("Student", String.Empty)
            @Html.ValidationMessageFor(model => model.Student)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Subject, "Subject1")
        </div>
        <div class="editor-field">
            @Html.DropDownList("Subject", String.Empty)
            @Html.ValidationMessageFor(model => model.Subject)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
